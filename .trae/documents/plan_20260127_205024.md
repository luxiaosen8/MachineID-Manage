## 执行计划：仅构建 Windows

### 1. 修改工作流配置
**文件**: `.github/workflows/release.yml`

**修改内容**:
- 移除 `strategy.matrix.os`（删除 Ubuntu 和 macOS 构建）
- 只保留 `windows-latest` 构建
- 简化整个工作流，去掉平台条件判断
- 移除 Ubuntu/macOS 相关的步骤和制品下载

### 2. 代码质量确认
- ✅ Rust 后端代码结构良好，使用 `#[cfg(windows)]` 确保 Windows 专有功能
- ✅ 前端 JavaScript 逻辑清晰，无明显问题
- ✅ 无需修改代码，工作流配置是唯一问题

### 3. 版本与推送
- 递增版本号至 1.3.7
- 提交并推送更改
- GitHub Actions 自动触发新构建

### 4. 预期结果
- 只构建 Windows 版本（`.msi` 和 `.exe`）
- 构建速度更快，成功率更高