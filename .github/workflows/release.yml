name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  NODE_VERSION: '20'

jobs:
  publish:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, ubuntu-22.04, windows-latest]

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y webkit2gtk4.0 libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev

      - name: install frontend dependencies
        run: |
          cd src-tauri
          npm install

      - name: Build and Release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "MachineID-Manage ${{ github.ref_name }}"
          releaseBody: |
            # MachineID-Manage ${{ github.ref_name }}

            ## About / 关于
            MachineID-Manage is a Windows MachineGuid management tool built with Rust + Tauri 2.
            MachineID-Manage 是一款基于 Rust + Tauri 2 开发的 Windows 机器码管理工具。

            ## What's New | 本次更新
            ### ✨ New Features | 新功能
            - **Multi-language Support** | **多语言支持**
              - Added complete i18n system with auto locale detection
              - 添加完整的 i18n 系统，支持自动检测系统语言
              - Supports English and Simplified Chinese (zh-CN)
              - 支持英文和简体中文

            ### 🐛 Bug Fixes | Bug 修复
            - **GitHub Actions Build Fix**
              - Fixed npm install directory for Tauri 2
              - Fixed workflow trigger conditions
              - 修复 GitHub Actions 构建失败及触发问题

            ## Features | 功能特性
            - 📖 Read MachineGuid | 读取机器码
            - 💾 Backup Management | 备份管理
            - 🔄 Restore Backup | 恢复备份
            - 🎲 Random Generation | 随机生成
            - 🔧 Custom Replace | 自定义替换
            - 📋 Copy Function | 复制功能

            ## Warning | 警告
            ⚠️ Modifying Windows Registry carries risks! Please backup first!
            ⚠️ 修改注册表存在风险！请务必先备份！

            ---
            View full documentation: https://github.com/luxiaosen8/MachineID-Manage
          releaseDraft: false
          prerelease: false
