name: Build and Release

on:
  push:
    branches:
      - master
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  publish:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: --bundle dmg,zip
          - platform: ubuntu-22.04
            args: --bundle deb,appimage
          - platform: windows-latest
            args: --bundle msi,zip

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: install dependencies (ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev javascriptcoregtk-4.1

      - name: install tauri-cli
        run: cargo install tauri-cli --version "^2.0.0"

      - name: Build and Release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_type == 'tag' && github.ref_name || format('v1.2.3-beta-{0}', github.sha) }}
          releaseName: "MachineID-Manage ${{ github.ref_type == 'tag' && github.ref_name || 'v1.2.3-beta' }}"
          releaseBody: |
            # MachineID-Manage Release

            ## About / 关于
            MachineID-Manage is a Windows MachineGuid management tool built with Rust + Tauri 2.
            MachineID-Manage 是一款基于 Rust + Tauri 2 开发的 Windows 机器码管理工具。

            ## What's New | 本次更新
            - ✨ Multi-language Support (en/zh-CN) | 多语言支持
            - 🚀 Standardized Tauri 2 CI/CD Workflow | 标准化 Tauri 2 构建流
            - 🐛 Fixed dependencies and path issues | 修复依赖与路径问题

            ---
            View full documentation: https://github.com/luxiaosen8/MachineID-Manage
          releaseDraft: false
          prerelease: ${{ github.ref_type != 'tag' }}
          args: ${{ matrix.args }}
